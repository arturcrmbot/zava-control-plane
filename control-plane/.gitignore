node_modules
dist
.env
.env.local
playwright-report
test-results

# Compiled TS artifacts (we build via Vite; any .js in src/ is stale tsc output)
src/**/*.js
src/**/*.js.map
src/**/*.d.ts

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
