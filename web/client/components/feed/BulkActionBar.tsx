// STUB — replaced by Task 32's real implementation.
export default function BulkActionBar({
  selectedIds: _selectedIds, onCleared: _onCleared,
}: {
  selectedIds: string[];
  onCleared: () => void;
}) {
  return null;
}
