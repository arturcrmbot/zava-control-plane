# Graduation — {{DOMAIN_DISPLAY_NAME}} ({{DOMAIN_NAME}})

> Generated by `compose-domain` on {{TIMESTAMP_UTC}}.
> Sandbox: `tools/scratch/compose-domain/{{RUN_ID}}/`

This file lists every hand-edit the engineer must apply to graduate the
sandbox into the real trees. The skill itself never edits any of these
files. Apply each section in order. Run the smoke command at the end.

If anything breaks during graduation, **the bug is in the SKILL.md** —
revert the graduation, fix the relevant SKILL.md (compose-domain or one
of the sub-skills), delete the sandbox, re-invoke, re-graduate.

---

## §1 — Copy these files

| Sandbox | Real tree |
|---|---|
{{COPY_TABLE_ROWS}}

## §2 — Edit `function_app.py`

Add the imports at the top of the file:

```python
{{FUNCTION_APP_IMPORTS}}
```

Add the orchestrator decorator alongside the existing
`ExpenseClaimOrchestrator` / `HiringOrchestrator`:

```python
{{FUNCTION_APP_ORCHESTRATOR_DECL}}
```

Add the activity decorators alongside the existing
`*_activity_trigger` functions:

```python
{{FUNCTION_APP_ACTIVITY_DECLS}}
```

## §3 — Edit `api/functions/workflows/activities.py`

Add the imports at the top:

```python
{{ACTIVITIES_IMPORTS}}
```

Add the activity functions:

```python
{{ACTIVITIES_FUNCTIONS}}
```

## §4 — Edit `api/functions/graphs/__init__.py`

Add the new `build_*` exports:

```python
{{GRAPHS_INIT_EXPORTS}}
```

## §5 — Edit `api/server/services/simulator_orchestrator.py`

Add the new spawn helper, mirroring `spawn_hiring_workflow`:

```python
{{SIMULATOR_SPAWN_FUNCTION}}
```

And register a route at `api/server/routes/simulator.py`:

```python
{{SIMULATOR_ROUTE}}
```

## §6 — Edit `api/server/services/blueprint_inventory.py`

Add to the `DOMAINS` list:

```python
{{INVENTORY_DOMAIN_ENTRY}}
```

## §7 — Edit `api/shared/constants.py`

Lift the per-phase timeouts out of the orchestrator file:

```python
{{CONSTANTS_DECLARATIONS}}
```

Then update the orchestrator's import:

```python
from api.shared.constants import (
{{CONSTANTS_IMPORTED_NAMES}}
)
```

…and remove the local `timedelta(...)` declarations.

## §8 — Smoke test

```bash
make test
make boot                                         # if not already running

curl -X POST http://localhost:3001{{SIMULATOR_ROUTE_PATH}} \
     -H 'Content-Type: application/json' \
     -d '{}'
```

Watch `/api/blueprint/stream`. Expected FleetEvent sequence:

```
{{EXPECTED_EVENT_SEQUENCE}}
```

If the sequence does not appear, revert this graduation, fix the SKILL.md
that produced the wrong output, re-invoke `compose-domain`, re-graduate.
