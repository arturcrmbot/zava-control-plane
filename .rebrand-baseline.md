# Rebrand baseline — refactor/rebrand-zava

Captured 2026-05-08 before any text substitution.

## Codename mapping (deviation from plan)

Plan was written for `Contoso`. **Executing as `Zava`** per user instruction
on 2026-05-08. All TASK-XXX `Contoso` references in the plan map to `Zava`.

| Old token | New token |
|---|---|
| `WPP Control Plane` | `Zava Control Plane` |
| `wpp-control-plane` | `zava-control-plane` |
| `wpp-control-plane-py` | `zava-control-plane-py` |
| `wpp-control-plane-poc1` | `zava-control-plane-poc1` |
| `wpp-control-plane-poc3-ai-agency` | `zava-control-plane-poc3-ai-agency` |
| `wpp.skill`, `wpp.tool.*`, `wpp.fleet_manager.*` | `zava.skill`, `zava.tool.*`, `zava.fleet_manager.*` |
| `WPP-HR` | `Zava-HR` |
| `London-WPP`, `UK-WPP` | `London-Zava`, `UK-Zava` |
| `WPP careers feed` | `Zava careers feed` |
| `WPP Code of Conduct` | `Zava Code of Conduct` |
| `WPP Talent` | `Zava Talent` |
| `WPP review` | `Zava review` |
| `WPP-supplied`, `WPP's 3,430-claim` | `Zava-supplied`, `Zava's 3,430-claim` |
| Azure RG `contoso-control-plane-demo` (planned) | `zava-control-plane-demo` |
| Azure ACR `blueprintacrcontosodemo` (planned) | `blueprintacrzavademo` |

## TASK-001 — baseline test pass count

`make test` on `refactor/rebrand-zava` at branch HEAD (parent: `2be6676d`):

```
1 failed, 715 passed, 28 skipped, 5 deselected, 1 warning in 96.74s
```

The 1 failure is the **known order-flake**
`tests/api/server/services/test_audit_chain.py::test_verify_route_reflects_real_chain_state`
documented in `/memories/repo/wpp-pre-existing-test-failures.md` — passes in
isolation, fails when run after `test_portal_voice` due to the
`sys.modules.pop("api.server.state")` AppState re-mint pattern. Not a regression
from rebrand. Acceptance bar: post-rebrand suite must show **same 1 failure +
715 passed + 28 skipped + 5 deselected** (or fewer failures).

## TASK-002 — pre-rebrand deployed bundle hash

Captured from `https://blueprint.jollystone-c036938d.swedencentral.azurecontainerapps.io/`:

```
pre_bundle=index-CLF_m3Ti.js
```

REQ-003 acceptance: post-rebrand redeploy must yield `post_bundle != pre_bundle`.

## TASK-004 — Azure subscription

`az account show --query name -o tsv`:

```
MCAPS-Hybrid-REQ-67826-2023-arzielinski
```

Same subscription owns the existing `project-apex-demo` RG used by the
blueprint container app.

## SQLite regeneration command (TASK-022)

After `rm data/.eval/store.sqlite data/portal/magic_links.sqlite`:
- `data/.eval/store.sqlite` is recreated by `api.server.eval.store.EvalStore.__init__`
  on first import (lazy schema bootstrap).
- `data/portal/magic_links.sqlite` is recreated by
  `api.server.routes.portal_magic_links` module on first request that touches it.

No dedicated seed script needed; the next `make dev` (or test run) recreates both.

## TASK-044 — post-rebrand deployed bundle hash

```
post_bundle=index-CEyGNxHY.js
```

REQ-003 verification: `pre_bundle` (`index-CLF_m3Ti.js`) != `post_bundle`
(`index-CEyGNxHY.js`) — **PASSED**.

New container app FQDN: `https://blueprint.jollygrass-c41bb8b9.swedencentral.azurecontainerapps.io`
- HTTP 200, sub-200ms TTFB
- `/api/health` returns HTTP 200
- Resource group: `zava-control-plane-demo`
- ACR: `blueprintacrzavademo.azurecr.io`
- Container Apps env: `blueprint-env` (auto-provisioned with workspace `workspace-zavacontrolplanedemoH6hm`)

## TASK-047 — old RG cleanup (executed early on 2026-05-08)

User opted to skip the 7-day soak. Old `project-apex-demo` resource group
deletion initiated 2026-05-08 13:13 BST:

```
az group delete -n project-apex-demo --yes --no-wait
# state: Deleting
```

Resources removed (12 total):
- Container app `blueprint` (old FQDN `blueprint.jollystone-c036938d.swedencentral.azurecontainerapps.io`)
- Container Apps env `blueprint-env`
- ACR `blueprintacrapexdemo` (with old WPP-tagged container images)
- Foundry/AI workspace `apexdemo62525` + companion resource (incl. all WPP-era OTEL traces and any saved tracing queries — RISK-002 self-resolved by RG delete)
- Log Analytics workspaces `workspace-projectapexdemoS5pa`, `workspace-projectapexdemoPGd8`
- Cognitive services: `apex-demo-doc-intelligence` (FormRecognizer), `apex-demo-speech` (SpeechServices)
- Communication services: `apex-demo-acs`, `apex-demo-email` + managed domain

Side effects accepted:
- `local.settings.json` (gitignored) still references the now-404
  `https://apex-demo-speech.cognitiveservices.azure.com` endpoint. Update
  locally if/when speech is needed for dev — not a deploy concern.

Empty leftover RG `cp-py-demo-rg` (likely from earlier rebrand experiment)
left untouched — out of scope for this plan; flag for follow-up if it
remains empty.
