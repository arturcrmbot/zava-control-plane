# Electronics recordings

This directory is the electronics-owned curated recordings root
(`RecordingSources.curated_dirs`). It intentionally starts empty: the
Electronics pack does not ship fabricated demo recordings. Curated
reference recordings can be added here later without any change to
`manifest.py`.
